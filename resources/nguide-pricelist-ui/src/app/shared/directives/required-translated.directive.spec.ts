import { RequiredTranslatedDirective } from './required-translated.directive';

describe('RequiredTranslatedDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredTranslatedDirective();
    expect(directive).toBeTruthy();
  });
});
